import 'package:flutter/material.dart';

class OneLineDiaryScreen extends StatelessWidget {
  const OneLineDiaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'One Line Diary View',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}