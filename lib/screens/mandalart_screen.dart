import 'package:flutter/material.dart';

class MandalartScreen extends StatelessWidget {
  const MandalartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Mandalart View',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}